# 云游江苏 · 项目交接文档

> 给下一位接手的 AI / 团队成员。读完这份文档 + 项目目录，即可快速接手。
> 最后更新：2026-08-24 23:14

---

## 一、项目是什么

- **名称**：云游江苏 · 把山水与味道讲给你听
- **定位**：面向大学生/年轻人的江苏文旅网站，让用户几分钟内"云游"江苏、放松一下
- **参赛**："建行杯"江苏大学生创新大赛（2026）高教主赛道 · 本科生创意组，江风叙物队
- **核心叙事**：水土 → 物产 → 饮食 → 文化 的地理叙事线（这是"江风叙物队"地理师范生的护城河）
- **设计基调**：松弛、治愈、年轻、东方、诗意；深色水墨 + 暖金；不做攻略站/GIS 工具风

## 二、技术栈与文件结构

**技术**：纯前端 HTML/CSS/JS + Python `server.py`（标准库 ThreadingHTTPServer）。**无框架、无构建、无数据库**，数据即 JS 全局变量。

**位置**：`C:\Users\wsvgs\Desktop\jiangsugeo\`

| 文件 | 作用 |
|---|---|
| `index.html` | 主页（4 视图：主菜单 / 地图云游 / 城市详情 / 关于本站） |
| `admin.html` | 后台管理（城市内容 / BGM 管理 / 站点信息，**无密码开放**） |
| `yunyou_style.css` | 样式（已重构：25 个 CSS token + 移动优先） |
| `yunyou_script.js` | 前端逻辑（视图切换 / BGM / 地图 / 城市渲染） |
| `city_data.js` | 13 市数据：每市 3 风景 + 3 美食 + 2 故事（字段 `t`/`d`/`img`，城市级 `name`/`full`/`tagline`/`lng`/`lat`） |
| `jiangsu_map.js` | 江苏地图 SVG 边界数据（`__JIANGSU_MAP__`） |
| `songs.js` | BGM 列表（`__SONGS__`，默认播第一首） |
| `site_config.js` | 站点文案（`title`/`subtitle`/`version`/`copyright`） |
| `server.py` | HTTP 服务 + 数据 API + 原子写入 + 自动备份 |
| `images/`、`bg/`、`music/` | 图片、主视觉背景、音频 |
| `backup/` | 自动备份（每次后台保存留一份，保留 20 份）+ 手动回滚点 |

**数据流**：后台 admin 编辑 → `server.py` 整包写回 `*.js` 文件 → 前台通过 `var __XXX__` 全局变量读取。

## 三、当前状态（今天已完成）

1. **13 市数据齐全**（每市 3 风景 + 3 美食 + 2 故事，地理叙事视角撰写）
2. **全站配图 87/104**：Pexels CC0（免费商用），17 条留空（Pexels 搜不到）
3. **站点信息可后台编辑**：标题 / 副标题 / 版本行 / 版权说明
4. **BGM 管理 + 选歌面板**（右上角点歌名切换）
5. **UI 重构 P0–P4 全部完成**：
   - P0 Token 化（25 个 CSS 变量）
   - P1 配色（深色水墨 + 暖金，主色 `#6fb8d8` 水墨青、强调 `#e0bf85` 暖金）
   - P2 字体（马善政楷书品牌字 + 系统无衬线正文 16px）
   - P3 移动优先（断点 992/768/576 + 触控 44px + 100dvh + 刘海屏安全区）
   - P4 无障碍（focus-visible / reduced-motion / 地图 aria / tab role）
6. **城市页图片预加载**：切换 tab 先加载全部图再一次性渲染（避免逐张蹦出）

## 四、待办 / 未决

1. ~~admin 保存报 `Permission denied`~~（**已修复** 2026-08-26）：根因是 server 进程跑久了文件句柄异常，**重启 server 即恢复**（解法：任务管理器关掉 python.exe 重启 start_yunyou.bat 或让 AI 重启）。附带修复：备份机制改为 `backup/latest_<文件名>` 固定名覆盖（原"保留 N 份+删除旧档"在本环境沙箱里 os.remove 被拦截 `SAFE_DELETE_FAIL_CLOSED`，旧备份删不掉导致堆积）。
2. **主界面方向未定**：用户觉得当前背景图不够"一眼不一样"。已讨论过方向（情绪入口"今天想去哪儿发会儿呆"、随机漫游"带我随便逛逛"、概念"会记得你的地方"），但**未落地**，等团队拍板。
3. **17 条未配图条目**：如连云港葛根粉、淮安周恩来故里、宿迁项王故里等（Pexels 无图）。可用文字卡片补位或留空。
4. **tab 横向滑动过渡**：已出方案（风景→美食→故事的"翻篇"轻滑动），未做。
5. `music/d44a02bdd3.wav` 是 10 字节无效残留文件，可删。

## 五、关键决策与约定（必须遵守）

- **战略**：不跟视频拼画面，跟视频拼"交互 + 人情味"。创意归人（团队），AI 只做落地实现。
- **配色**：保持深色水墨，冷青 → 青+暖金平衡（用户已确认）
- **字体**：正文系统字体栈（用户已确认，不引入外部字体文件）
- **后台无密码**：用户明确要求（方便组员协作），不要加回密码
- **版权合规**：只用 Pexels CC0 / AI 生成 / 有授权的图；**禁用 B 站截图**（用户问过，明确不合法）
- **图片原则**：宁缺毋滥，配不到就留空，别硬塞错图
- **动画克制**：只动画 transform/opacity，别炫技
- **中国语境**：港/澳/台表述合规；涨红跌绿（本项目暂不涉及金融）

## 六、环境与运维

- **端口**：8081（`python server.py 8081`）
- **启动**：桌面 `start_yunyou.bat`（菜单版：1 启动+cpolar / 2 仅本地 / 3 备份到D盘 / 4 开前台 / 5 开后台）
- **访问**：前台 `http://localhost:8081/`，后台 `http://localhost:8081/admin`
- **回滚点**：`backup/ui_refactor_20260824_223635/`（7 个关键文件的 UI 改造前完整备份）
- **Pexels key**：项目根目录 `.pexels_key`（本地文件，勿外传）

## 七、踩坑记录（给下一位的提醒）

1. **绝不能用 `open(path,'w')` 打开目标文件本身来"读自己再写回"** —— `'w'` 会立即截断文件为 0 字节。曾因此清空过 city_data.js（已恢复）。模拟写入必须先 `open(path,'r')` 读全量进内存再写。
2. **Git Bash 的 awk/grep/sed 会把 CRLF 自动转 LF**，判断行尾必须用 Python `rb` 读原始字节。
3. **`bash -c` 内嵌复杂 Python 易转义炸裂**，长脚本一律写独立 `.py` 文件再运行。
4. **Windows 原生 curl 不识别 `/tmp` 路径**；Python `subprocess` 调 curl 偶发空输出，必要时用 bash 直接 curl。
5. **Pexels 对中国二三线小众内容覆盖极差**（搜"花果山"出阿拉木图、搜"地锅鸡"出活鸡），alt 过滤的 banned 词必须包含常见国外地标，且全站 URL 去重。
6. **当前模型不支持读图**（Read 图片被过滤），图片效果只能靠用户确认 + alt 描述。
7. **回收站写入受限**：SHFileOperationW 在该沙箱返回错误码 2 且不写回收站（实际直接删除），要"进回收站"只能用户手动删。
8. **admin 保存 Permission denied** 的根因排查走 server.py 的 `atomic_write`（写 .tmp → os.replace → 降级直写）+ `backup_file`（写 backup/）几步，注意别重复踩"读自己写回"的坑。
